# Project agent memory

This file is the project's committed home for project-intrinsic agent knowledge: build, test, release, architecture, and sharp-edge notes that should travel with the code.

- Walking-skeleton implementation has begun; the authoritative setup, commands, and current scope are in `README.md` and root `package.json`. Accepted product contracts, external unknowns, and launch risks live under `docs/`.
- The captain approved the checksum-pinned community Omens 3.8 recipe and recorded policy decisions in `docs/risks-and-decisions.md`; its Phase 2 import must be test-first and separately reviewed. Do not commit the source recipe or generate a snapshot before that review.
- The v8.2.0 public English card input/schema and the observed 2026-08-04 official Card Vault Omens product response are build-time evidence inputs; follow the evidence workflow in `README.md`, never commit upstream bytes, and never add runtime fetches. The Card Vault raw checksum is dated evidence only; canonical official membership is the durable authority.
- Pinned public evidence must be obtained and checksum-verified when absent, never waived because a worker is isolated; only captain-held private recipe evidence is genuinely unavailable to workers. That private unavailability neither authorizes acceptance nor blocks handoff: complete worker validation, push, open the PR, verify and name its exact full remote head, and report private four-source acceptance as awaiting captain measurement. The request must name that head, and the captain must report the exact head last measured so unpublished work cannot deadlock acceptance.
- The approved boundary is a platform-independent TypeScript engine plus browser, Cloudflare Worker/Durable Object adapter, shared contracts, and a reviewed versioned set snapshot; see `docs/architecture.md`.
- Preserve recipe `common|rare|mythic` only at ingest and translate once to FaB-native common/rare/Majestic plus C/R/M; unknown labels remain generic parser failures. Rarity output must retain the authoritative per-row source-order code sequence including duplicates; its separately named first-observed unique set is only a lossy classification view. The pinned broad upstream domain is `C|R|M|P|V|T|L|S|B|F`; public guards pin 482 OMN official-base rows and 493 all-official retained rows differing by 11 IAR V rows. Scoped public-candidate and captain-held mapped sequence aggregates are authoritative in `docs/rules-and-collation.md`; its completed partial Omens product-policy classification is 209 mapped draftable, 9 unmapped IAR excluded only by captain contract, and 42 other unmapped identities unclassified (real four-source acceptance pending). Preserve its evidence-versus-product attribution and bounded cross-set guidance, including the captain-supplied i'Arathael product context and separately observed exact IAR222 source-name boundary at its [IAR product context and name boundary](docs/rules-and-collation.md#iar-product-context-and-name-boundary). Validated recipe pool references now resolve only through exact same-source CustomCards ownership, collector-first reconciliation, and exact draftable eligibility; all weighted layout positions now preserve source order/multiplicity and explicit recipe-structural common-rarity, fixed-rare, rare-or-majestic, and rainbow-foil roles while referencing those opaque resolved pools. Exact source-order integer collation-weight tables are compiled through those capabilities, and exact bounded-ticket lookup of their capability-owned layout/pool references is implemented; both remain captain-acceptance-pending. One validated uint32 sample now composes the platform-independent engine's unbiased mapper with the exact capability-owned layout or pool bound, returning immutable retry-or-selection only. A registered immutable pack-local projection now initializes all 11 exact pool tables and removes one exact capability-owned identity only from its exact pool with cumulative weights recompiled; this implements repeated same-pool `withReplacement=false` only. Dynamic bounded-ticket lookup now selects one remaining identity from that exact same-pool state; its real four-source acceptance remains captain-pending. One uint32 sample now composes with that exact current state and pool into immutable retry-or-atomic same-pool selection-and-removal; its real four-source acceptance remains captain-pending. One selected layout now initializes a fresh immutable all-11-pool plan with cursor zero; one finite caller-supplied uint32 batch now composes only the engine batch mapper, exact layout ticket lookup, and that registration into immutable need-more-or-plan output with preserved consumed count. One following finite batch now transitions exactly the current recipe-structural plan position and one identity draw through exact dynamic-pool selection/removal into a new immutable cursor+1 plan. Every registered plan now retains an independently owned immutable source-order selection history from empty through 14 terminal records, preserving the exact accepted position and official-identity references; its real four-source acceptance remains captain-pending. A build-time-only immutable card-presentation projection preserves one caller-selected official identity, upstream printing, and Card Vault face source boundary without making those selections; `docs/rules-and-collation.md` owns its evidence boundary. Complete 14-position pack construction preserves finite caller batches, immutable continuation/history/pool state, and no entropy ownership (callers own it); normal/Rainbow-Foil cross-pool identity overlap is legal without a policy switch or deduplication. The deterministic PCG source is replay-only, not cryptographic. `packages/draft` owns pure draft transitions only. `apps/web` now composes those transitions with the engine mapper into a single-player eight-seat three-round browser draft dealing real Omens cards from the reviewed set snapshot (`@draft-table/set-omens/snapshot`), collated the reviewed way: weighted layout choice, then weighted per-position draws without replacement within each pool, preserving cross-pool normal/Rainbow-Foil overlap. `packages/set-omens/scripts/build-set-snapshot.mjs` generates that snapshot from the four checksum-pinned sources and it is committed, so CI and the browser never need the recipe; `set-snapshot.ts` owns the validator the generated module runs at load, and `test:set-snapshot-evidence` proves the committed bytes still match the evidence. The snapshot holds names, pitch, rarity, each identity's exact official Card Vault image URL, and the exact upstream type tokens, never image or upstream bytes. Upstream mixes class, talent, type, and subtype into one token list, so the validator derives `cardType` and `cardClass` from those tokens rather than reading them, and refuses an unreviewed token; 60 identities legitimately have no class. `apps/web/src/pool.ts` owns pure pool grouping (set number, class, colour, type) and must never drop, duplicate, or reorder a card out of collector order; `packages/contracts` owns shapes both the browser and room server need and takes a shape only when a second real caller needs it; its `room-code.ts` mints eight-symbol Crockford-Base32 codes from exactly five caller-supplied entropy bytes as a bijection, reads them by Crockford's rules (case-insensitive, I/L as 1, O as 0, hyphens dropped, U refused), and never echoes a rejected code into a message, log, or stack. `apps/server/src/index.ts` is the whole Worker router: two dynamic routes (`POST /api/rooms`, `GET /api/rooms/<code>/socket`) and static assets for everything else. It refuses non-same-origin (including originless), wrong-method, wrong-media-type, oversized (declared, understated, or undeclared), non-JSON-object, non-upgrade, and never-mintable-code traffic before any object is woken; treats a 409 from the object as the only authority on a taken code and mints another, bounded; forwards a socket upgrade untouched because a rebuilt upgrade does not survive; and never echoes a body or a code into a response. Per-caller rate limits and `GET /api/health` are deliberately absent. `apps/server/src/room.ts` is the room Durable Object, re-exported from the Worker entry. It owns the initialize transaction (201 with the code and a one-time host claim, 409 ever after and the router's only "taken" signal, strict known-field config reading with product-contract defaults, secrets stored solely as salted SHA-256 verifiers compared in constant time, snapshot persisted before any answer or broadcast) and the hibernating-socket lobby gate: upgrades are accepted silently; the first message must be a well-formed hello (password gate first — wrong and missing answer identically with stateVersion 0 — then credential reclaim, the one-time host claim, and validated display names); every unauthenticated refusal is one generic error then close 4001 with no mutation; a successful hello mints a 256-bit credential returned exactly once, seats fill 0–7 then the spectator row to cap 16, and the newest connection per identity supersedes the older (close 4000) with a generation guard so a superseded socket's close cannot mark the newer one dead. Every handler runs whole inside `blockConcurrencyWhile`, because the platform's input gate holds only across storage awaits and these handlers also await crypto between snapshot read and write — without it two hellos can interleave, lose a participant, or double-spend the host claim (race-tested). The one alarm serves two appointments: recorded `abandonAt` (set at creation and on the last goodbye, cleared by any join, early wakes rebook) deletes a nobody-connected lobby whole; with `abandonAt` null a standing liveness sweep reconciles connected flags against live socket attachments, because a deploy kills sockets without close events — a join books the sweep, the sweep rebooks itself, and a ghost lobby starts its abandonment clock. Handlers rebuild all state from storage and socket attachments because hibernation forgets memory. The lobby commands — update_profile, whole-object update_config over the four safe options, move_participant with swap semantics (the first manual move spends the pending shuffle and announces it), set_seat_randomization (server-owned randomize_now shuffles seated participants across their own occupied positions with rejection-sampled uniform indices), remove_participant (never the host as target), leave (immediate whole-room deletion when the last participant departs on purpose), and resync — share one commit path: persist, ack the sender with the new stateVersion, broadcast to everyone else; a withdrawn identity's credential dies with its place and its sockets close. `apps/server/src/packs.ts` mirrors `apps/web/src/cards.ts`'s reviewed snapshot collation deliberately (no shared export is reviewed yet) minus display labels. start_draft (host, exact expectedStateVersion, 1–8 seated) applies any pending shuffle, compacts positions into the ring, deals all three rounds up front, and persists before announcing; queue_pick maps DraftRuleError codes to stable protocol errors and broadcasts only seatId+hasQueued; the confirmation's five-second floor alone makes it one-shot (the recorded deadlineAccelerated flag is pause/resume bookkeeping, not a guard); the deadline alarm resolves via the engine's resolveTimeout with fallbacks for exactly the unqueued seats, then routes phase → review (60 s, pile face up) → picking or complete (one-hour TTL); disconnect/reconnect flow through the engine's presence transitions and recompute only the confirmation (a reconnect in the lobby books the liveness sweep, but a started room's booked deadline is never overwritten — guard in hello); privateView hides the pool while picking under poolHidden and never carries another seat's cards; mid-draft strangers are spectators or refused spectators_disabled. The router maps the object's 400 to its own malformed_request without echoing either body; storage, time, entropy, and the socket pair arrive through narrow injected slices so tests drive the whole gate without a Cloudflare runtime. `apps/web/src/fabrary.ts` owns the pure DT-7 Fabrary hand-off: a pre-filled public import link plus the always-offered copyable list, one identifier per physical copy with normal/Rainbow-Foil collapsed, never a private authenticated endpoint, never a network call, and never a promise to sign in or pick a hero; `OMENS_SNAPSHOT_IMAGE_ORIGIN` pins the one origin art may come from, the validator refuses any image that is not that identity's own rendition on it, and the page's content security policy pins the same origin. Adding any `packages/set-omens/src/*.ts` module also requires adding it to the canonical allowlist in `test/package-boundary.test.mjs`. The client owns no card evidence and no entropy, because the browser supplies `crypto.getRandomValues`. `apps/web/src/protocol.ts` (pure envelopes, strict frame reading, fastest-round-trip clock estimation, bounded backoff) and `apps/web/src/room-client.ts` (the one stateful connection driver: per-room stored credential, one shared hello builder whose first ack retires the one-shot claim and name, resync on version gaps, growing reconnect waits, and no reconnection over refusal, supersession, or a door that never opened) are the only client files allowed scripted networking — `shell.test.mjs` confines fetch/XHR/WebSocket to exactly them and they name no origin; the page wires none of it yet. Its deterministic build transpiles each workspace module and inlines them behind a small CommonJS registry: separate workspace modules legitimately declare the same private names, so flat concatenation would silently break them, and an unresolvable specifier fails the build instead of reaching the browser. Recipe-structural roles are not physical slots. Under DT-9 the captain accepted self-hosting the card art: `scripts/migrate-card-images.mjs` copies every image the snapshot names from the pinned official origin into gitignored `apps/server/card-images/`, records each source/sha256/byte length in committed `apps/server/card-image-manifest.json`, and refuses an unsafe identifier, an off-origin source, a redirect, or a body that is not a real WebP served as one. Never commit an image byte, never re-encode a copy, and treat uploading to the serving host as a separate deliberate step the captain authorizes.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.

- Contracts named for a specific guard must semantically bypass that guard while preserving surrounding/source checks and prove the contract fails; deletion-only mutations are insufficient because another layer may mask the guard.
- Command and mutation evidence must assert that the intended command and named contract actually executed and produced its exact expected success/failure marker; usage errors, wrong environment variables, or arbitrary nonzero exits are invalid evidence even if reported as green.
- Mutation proofs for focused guard contracts must execute the **named** contract against a
  semantically modified copy of the production module, not a re-implemented fixture. Parameterize
  only the module path through a test-only environment variable that defaults to the production
  import, run the contract by exact name via `--test-name-pattern`, and assert the exact exit
  status, one execution marker, one `not ok` carrying the contract name, and one specific failure
  line. No loaders, interception, or shared harnesses. See
  `packages/set-omens/test/official-suffix-foiling-classification.test.mjs` for the reference
  implementation. The module path must target a file-local OS-temp isolated canonical-source
  snapshot that cleans setup and body failures and preserves dependency resolution. Every semantic
  mutation must assert successful TypeScript checking of that isolated mutated snapshot before its
  expected failing contract. Never write mutation modules beside production source; loaders,
  interception, and shared mutation harnesses remain prohibited. Existing inline-duplicate proofs
  are follow-up candidates, not blessed.
- Assignment consults the prototype chain whereas own-property definition does not; exported error classes must define own instance fields with a module-captured `Object.defineProperty` and freeze their constructor and prototype at module load. Freezing an array does not freeze inherited accessors: handed-out immutable arrays require every entry to be an own data property, and `Object.isFrozen` says nothing about entries never written as own properties. Capture every load-bearing intrinsic before hostile property reads and keep the full-composition cross-attacks enforced by finite-batch collation-plan contracts.
- When findings share a root cause, test each fix against the other findings' attacks; individually plausible fixes can be mutually defeatable. Keep the cross-attack coverage with the owning contracts.
- Test allegedly redundant guards with value-preserving attacks that keep structure, equality, length, and frozen status apparently correct; an obviously invalid attack proves nothing about the narrower guard.
- Structural transition validity does not prove per-invocation freshness; when one call mints or returns multiple capabilities, enumerate every object and enforce and test independent freshness for each.
- Finite caller-batch composers that own the boundary snapshot length and elements exactly once inside the stable error boundary using prototype-safe own writes, pass only the immutable snapshot inward, bind consumed counts to the captured length, and never touch the caller object again.
