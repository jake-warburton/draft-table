import assert from "node:assert/strict";
import { spawnSync } from "node:child_process";
import { copyFileSync, mkdtempSync, readFileSync, readdirSync, rmSync, symlinkSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";
import test from "node:test";
import { fictionalCollationCapabilities } from "./fictional-collation-capabilities.mjs";

const moduleKey = "DRAFT_TABLE_TEST_FINITE_BATCH_PLAN_MODULE";
const finiteSourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url);
const exactPattern = (name) => `^${name.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}$`;

const finite_batch_collation_plan_defineOwnDataPropertyContract = "finite batch plan captures Object.defineProperty in finite-batch-collation-plan.ts", finite_batch_collation_plan_defineOwnDataPropertyMarker = "FINITE_BATCH_COLLATION_PLAN_DEFINEOWNDATAPROPERTY_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_defineOwnDataPropertyContract, async () => { console.log(finite_batch_collation_plan_defineOwnDataPropertyMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["defineProperty"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["defineProperty"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_DEFINEOWNDATAPROPERTY_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_DEFINEOWNDATAPROPERTY_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_DEFINEOWNDATAPROPERTY_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["defineProperty"] = original; } });
test("finite batch defineOwnDataProperty capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const defineOwnDataProperty: typeof Object.defineProperty = Object.defineProperty;", replacement = "const defineOwnDataProperty: typeof Object.defineProperty = ((...values: Parameters<typeof Object.defineProperty>) => Object.defineProperty(...values)) as typeof Object.defineProperty;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_defineOwnDataProperty-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_defineOwnDataPropertyContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_defineOwnDataPropertyMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_defineOwnDataPropertyContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_DEFINEOWNDATAPROPERTY_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const finite_batch_collation_plan_freezeContract = "finite batch plan captures Object.freeze in finite-batch-collation-plan.ts", finite_batch_collation_plan_freezeMarker = "FINITE_BATCH_COLLATION_PLAN_FREEZE_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_freezeContract, async () => { console.log(finite_batch_collation_plan_freezeMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["freeze"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["freeze"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["freeze"] = original; } });
test("finite batch freeze capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const freeze: typeof Object.freeze = Object.freeze;", replacement = "const freeze: typeof Object.freeze = ((value: object) => Object.freeze(value)) as typeof Object.freeze;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_freeze-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_freezeContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_freezeMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_freezeContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const finite_batch_collation_plan_getOwnPropertyDescriptorContract = "finite batch plan captures Object.getOwnPropertyDescriptor in finite-batch-collation-plan.ts", finite_batch_collation_plan_getOwnPropertyDescriptorMarker = "FINITE_BATCH_COLLATION_PLAN_GETOWNPROPERTYDESCRIPTOR_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_getOwnPropertyDescriptorContract, async () => { console.log(finite_batch_collation_plan_getOwnPropertyDescriptorMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["getOwnPropertyDescriptor"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["getOwnPropertyDescriptor"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_GETOWNPROPERTYDESCRIPTOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_GETOWNPROPERTYDESCRIPTOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_GETOWNPROPERTYDESCRIPTOR_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["getOwnPropertyDescriptor"] = original; } });
test("finite batch getOwnPropertyDescriptor capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const getOwnPropertyDescriptor: typeof Object.getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;", replacement = "const getOwnPropertyDescriptor: typeof Object.getOwnPropertyDescriptor = ((value: object, property: PropertyKey) => Object.getOwnPropertyDescriptor(value, property)) as typeof Object.getOwnPropertyDescriptor;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_getOwnPropertyDescriptor-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_getOwnPropertyDescriptorContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_getOwnPropertyDescriptorMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_getOwnPropertyDescriptorContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_GETOWNPROPERTYDESCRIPTOR_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const finite_batch_collation_plan_ownKeysContract = "finite batch plan captures Reflect.ownKeys in finite-batch-collation-plan.ts", finite_batch_collation_plan_ownKeysMarker = "FINITE_BATCH_COLLATION_PLAN_OWNKEYS_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_ownKeysContract, async () => { console.log(finite_batch_collation_plan_ownKeysMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Reflect, original = owner["ownKeys"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["ownKeys"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_OWNKEYS_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_OWNKEYS_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_OWNKEYS_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["ownKeys"] = original; } });
test("finite batch ownKeys capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const ownKeys: typeof Reflect.ownKeys = Reflect.ownKeys;", replacement = "const ownKeys: typeof Reflect.ownKeys = (value) => Reflect.ownKeys(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_ownKeys-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_ownKeysContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_ownKeysMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_ownKeysContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_OWNKEYS_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const finite_batch_collation_plan_isFrozenContract = "finite batch plan captures Object.isFrozen in finite-batch-collation-plan.ts", finite_batch_collation_plan_isFrozenMarker = "FINITE_BATCH_COLLATION_PLAN_ISFROZEN_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_isFrozenContract, async () => { console.log(finite_batch_collation_plan_isFrozenMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["isFrozen"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isFrozen"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isFrozen"] = original; } });
test("finite batch isFrozen capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isFrozen: typeof Object.isFrozen = Object.isFrozen;", replacement = "const isFrozen: typeof Object.isFrozen = (value) => Object.isFrozen(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_isFrozen-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_isFrozenContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_isFrozenMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_isFrozenContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const finite_batch_collation_plan_isSafeIntegerContract = "finite batch plan captures Number.isSafeInteger in finite-batch-collation-plan.ts", finite_batch_collation_plan_isSafeIntegerMarker = "FINITE_BATCH_COLLATION_PLAN_ISSAFEINTEGER_CONTRACT_EXECUTED";
test(finite_batch_collation_plan_isSafeIntegerContract, async () => { console.log(finite_batch_collation_plan_isSafeIntegerMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Number, original = owner["isSafeInteger"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isSafeInteger"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "FINITE_BATCH_COLLATION_PLAN_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "FINITE_BATCH_COLLATION_PLAN_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "FINITE_BATCH_COLLATION_PLAN_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isSafeInteger"] = original; } });
test("finite batch isSafeInteger capture mutation in finite-batch-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/finite-batch-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isSafeInteger: typeof Number.isSafeInteger = Number.isSafeInteger;", replacement = "const isSafeInteger: typeof Number.isSafeInteger = (value) => Number.isSafeInteger(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-finite_batch_collation_plan_isSafeInteger-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "finite-batch-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(finite_batch_collation_plan_isSafeIntegerContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + finite_batch_collation_plan_isSafeIntegerMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(finite_batch_collation_plan_isSafeIntegerContract)).length, 1); assert.equal(lines.filter((line) => line.includes("FINITE_BATCH_COLLATION_PLAN_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_tables_weakSetHasContract = "finite batch plan captures WeakSet.prototype.has in collation-weight-tables.ts", collation_weight_tables_weakSetHasMarker = "COLLATION_WEIGHT_TABLES_WEAKSETHAS_CONTRACT_EXECUTED";
test(collation_weight_tables_weakSetHasContract, async () => { console.log(collation_weight_tables_weakSetHasMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakSet.prototype, original = owner["has"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["has"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TABLES_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TABLES_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TABLES_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["has"] = original; } });
test("finite batch weakSetHas capture mutation in collation-weight-tables.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-tables.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakSetHas = Function.prototype.call.bind(WeakSet.prototype.has) as (set: WeakSet<object>, value: object) => boolean;", replacement = "const weakSetHas = ((set: WeakSet<object>, value: object) => WeakSet.prototype.has.call(set, value)) as (set: WeakSet<object>, value: object) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_tables_weakSetHas-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-tables.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_tables_weakSetHasContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_tables_weakSetHasMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_tables_weakSetHasContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TABLES_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_tables_arraySomeContract = "finite batch plan captures Array.prototype.some in collation-weight-tables.ts", collation_weight_tables_arraySomeMarker = "COLLATION_WEIGHT_TABLES_ARRAYSOME_CONTRACT_EXECUTED";
test(collation_weight_tables_arraySomeContract, async () => { console.log(collation_weight_tables_arraySomeMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array.prototype, original = owner["some"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["some"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TABLES_ARRAYSOME_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TABLES_ARRAYSOME_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TABLES_ARRAYSOME_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["some"] = original; } });
test("finite batch arraySome capture mutation in collation-weight-tables.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-tables.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arraySome = Function.prototype.call.bind(Array.prototype.some) as <Value>(array: readonly Value[], predicate: (value: Value) => boolean) => boolean;", replacement = "const arraySome = (<Value>(array: readonly Value[], predicate: (value: Value) => boolean) => Array.prototype.some.call(array, predicate)) as <Value>(array: readonly Value[], predicate: (value: Value) => boolean) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_tables_arraySome-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-tables.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_tables_arraySomeContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_tables_arraySomeMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_tables_arraySomeContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TABLES_ARRAYSOME_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_ticket_selection_arrayIsArrayContract = "finite batch plan captures Array.isArray in collation-weight-ticket-selection.ts", collation_weight_ticket_selection_arrayIsArrayMarker = "COLLATION_WEIGHT_TICKET_SELECTION_ARRAYISARRAY_CONTRACT_EXECUTED";
test(collation_weight_ticket_selection_arrayIsArrayContract, async () => { console.log(collation_weight_ticket_selection_arrayIsArrayMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array, original = owner["isArray"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isArray"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TICKET_SELECTION_ARRAYISARRAY_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TICKET_SELECTION_ARRAYISARRAY_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TICKET_SELECTION_ARRAYISARRAY_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isArray"] = original; } });
test("finite batch arrayIsArray capture mutation in collation-weight-ticket-selection.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-ticket-selection.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arrayIsArray: typeof Array.isArray = Array.isArray;", replacement = "const arrayIsArray: typeof Array.isArray = (value) => Array.isArray(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_ticket_selection_arrayIsArray-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-ticket-selection.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_ticket_selection_arrayIsArrayContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_ticket_selection_arrayIsArrayMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_ticket_selection_arrayIsArrayContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TICKET_SELECTION_ARRAYISARRAY_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_ticket_selection_isFrozenContract = "finite batch plan captures Object.isFrozen in collation-weight-ticket-selection.ts", collation_weight_ticket_selection_isFrozenMarker = "COLLATION_WEIGHT_TICKET_SELECTION_ISFROZEN_CONTRACT_EXECUTED";
test(collation_weight_ticket_selection_isFrozenContract, async () => { console.log(collation_weight_ticket_selection_isFrozenMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["isFrozen"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isFrozen"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TICKET_SELECTION_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TICKET_SELECTION_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TICKET_SELECTION_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isFrozen"] = original; } });
test("finite batch isFrozen capture mutation in collation-weight-ticket-selection.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-ticket-selection.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isFrozen: typeof Object.isFrozen = Object.isFrozen;", replacement = "const isFrozen: typeof Object.isFrozen = (value) => Object.isFrozen(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_ticket_selection_isFrozen-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-ticket-selection.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_ticket_selection_isFrozenContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_ticket_selection_isFrozenMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_ticket_selection_isFrozenContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TICKET_SELECTION_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_ticket_selection_isSafeIntegerContract = "finite batch plan captures Number.isSafeInteger in collation-weight-ticket-selection.ts", collation_weight_ticket_selection_isSafeIntegerMarker = "COLLATION_WEIGHT_TICKET_SELECTION_ISSAFEINTEGER_CONTRACT_EXECUTED";
test(collation_weight_ticket_selection_isSafeIntegerContract, async () => { console.log(collation_weight_ticket_selection_isSafeIntegerMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Number, original = owner["isSafeInteger"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isSafeInteger"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TICKET_SELECTION_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TICKET_SELECTION_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TICKET_SELECTION_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isSafeInteger"] = original; } });
test("finite batch isSafeInteger capture mutation in collation-weight-ticket-selection.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-ticket-selection.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isSafeInteger: typeof Number.isSafeInteger = Number.isSafeInteger;", replacement = "const isSafeInteger: typeof Number.isSafeInteger = (value) => Number.isSafeInteger(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_ticket_selection_isSafeInteger-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-ticket-selection.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_ticket_selection_isSafeIntegerContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_ticket_selection_isSafeIntegerMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_ticket_selection_isSafeIntegerContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TICKET_SELECTION_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_ticket_selection_floorContract = "finite batch plan captures Math.floor in collation-weight-ticket-selection.ts", collation_weight_ticket_selection_floorMarker = "COLLATION_WEIGHT_TICKET_SELECTION_FLOOR_CONTRACT_EXECUTED";
test(collation_weight_ticket_selection_floorContract, async () => { console.log(collation_weight_ticket_selection_floorMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Math, original = owner["floor"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["floor"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TICKET_SELECTION_FLOOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "COLLATION_WEIGHT_TICKET_SELECTION_FLOOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TICKET_SELECTION_FLOOR_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["floor"] = original; } });
test("finite batch floor capture mutation in collation-weight-ticket-selection.ts fails its exact named contract", () => { const sourcePath = new URL("../src/collation-weight-ticket-selection.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const floor: typeof Math.floor = Math.floor;", replacement = "const floor: typeof Math.floor = (value) => Math.floor(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_ticket_selection_floor-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "collation-weight-ticket-selection.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_ticket_selection_floorContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + collation_weight_ticket_selection_floorMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_ticket_selection_floorContract)).length, 1); assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TICKET_SELECTION_FLOOR_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_arrayEveryContract = "finite batch plan captures Array.prototype.every in pack-collation-plan.ts", pack_collation_plan_arrayEveryMarker = "PACK_COLLATION_PLAN_ARRAYEVERY_CONTRACT_EXECUTED";
test(pack_collation_plan_arrayEveryContract, async () => { console.log(pack_collation_plan_arrayEveryMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array.prototype, original = owner["every"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["every"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["every"] = original; } });
test("finite batch arrayEvery capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arrayEvery = Function.prototype.call.bind(Array.prototype.every) as <Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => boolean;", replacement = "const arrayEvery = (<Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => Array.prototype.every.call(array, predicate)) as <Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_arrayEvery-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_arrayEveryContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_arrayEveryMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_arrayEveryContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_arrayFindContract = "finite batch plan captures Array.prototype.find in pack-collation-plan.ts", pack_collation_plan_arrayFindMarker = "PACK_COLLATION_PLAN_ARRAYFIND_CONTRACT_EXECUTED";
test(pack_collation_plan_arrayFindContract, async () => { console.log(pack_collation_plan_arrayFindMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array.prototype, original = owner["find"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["find"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_ARRAYFIND_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_ARRAYFIND_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_ARRAYFIND_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["find"] = original; } });
test("finite batch arrayFind capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arrayFind = Function.prototype.call.bind(Array.prototype.find) as <Value>(array: readonly Value[], predicate: (value: Value) => boolean) => Value | undefined;", replacement = "const arrayFind = (<Value>(array: readonly Value[], predicate: (value: Value) => boolean) => Array.prototype.find.call(array, predicate)) as <Value>(array: readonly Value[], predicate: (value: Value) => boolean) => Value | undefined;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_arrayFind-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_arrayFindContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_arrayFindMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_arrayFindContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_ARRAYFIND_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_mapGetContract = "finite batch plan captures Map.prototype.get in pack-collation-plan.ts", pack_collation_plan_mapGetMarker = "PACK_COLLATION_PLAN_MAPGET_CONTRACT_EXECUTED";
test(pack_collation_plan_mapGetContract, async () => { console.log(pack_collation_plan_mapGetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Map.prototype, original = owner["get"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["get"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_MAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_MAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_MAPGET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["get"] = original; } });
test("finite batch mapGet capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const mapGet = Function.prototype.call.bind(Map.prototype.get) as <Key, Value>(map: Map<Key, Value>, key: Key) => Value | undefined;", replacement = "const mapGet = (<Key, Value>(map: Map<Key, Value>, key: Key) => Map.prototype.get.call(map, key)) as <Key, Value>(map: Map<Key, Value>, key: Key) => Value | undefined;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_mapGet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_mapGetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_mapGetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_mapGetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_MAPGET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_mapSetContract = "finite batch plan captures Map.prototype.set in pack-collation-plan.ts", pack_collation_plan_mapSetMarker = "PACK_COLLATION_PLAN_MAPSET_CONTRACT_EXECUTED";
test(pack_collation_plan_mapSetContract, async () => { console.log(pack_collation_plan_mapSetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Map.prototype, original = owner["set"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["set"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_MAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_MAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_MAPSET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["set"] = original; } });
test("finite batch mapSet capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const mapSet = Function.prototype.call.bind(Map.prototype.set) as <Key, Value>(map: Map<Key, Value>, key: Key, value: Value) => Map<Key, Value>;", replacement = "const mapSet = (<Key, Value>(map: Map<Key, Value>, key: Key, value: Value) => Map.prototype.set.call(map, key, value)) as <Key, Value>(map: Map<Key, Value>, key: Key, value: Value) => Map<Key, Value>;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_mapSet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_mapSetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_mapSetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_mapSetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_MAPSET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_weakMapGetContract = "finite batch plan captures WeakMap.prototype.get in pack-collation-plan.ts", pack_collation_plan_weakMapGetMarker = "PACK_COLLATION_PLAN_WEAKMAPGET_CONTRACT_EXECUTED";
test(pack_collation_plan_weakMapGetContract, async () => { console.log(pack_collation_plan_weakMapGetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakMap.prototype, original = owner["get"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["get"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["get"] = original; } });
test("finite batch weakMapGet capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakMapGet = Function.prototype.call.bind(WeakMap.prototype.get) as <Value>(map: WeakMap<object, Value>, key: object) => Value | undefined;", replacement = "const weakMapGet = (<Value>(map: WeakMap<object, Value>, key: object) => WeakMap.prototype.get.call(map, key)) as <Value>(map: WeakMap<object, Value>, key: object) => Value | undefined;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_weakMapGet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_weakMapGetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_weakMapGetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_weakMapGetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_collation_plan_weakMapSetContract = "finite batch plan captures WeakMap.prototype.set in pack-collation-plan.ts", pack_collation_plan_weakMapSetMarker = "PACK_COLLATION_PLAN_WEAKMAPSET_CONTRACT_EXECUTED";
test(pack_collation_plan_weakMapSetContract, async () => { console.log(pack_collation_plan_weakMapSetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakMap.prototype, original = owner["set"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["set"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["set"] = original; } });
test("finite batch weakMapSet capture mutation in pack-collation-plan.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakMapSet = Function.prototype.call.bind(WeakMap.prototype.set) as <Value>(map: WeakMap<object, Value>, key: object, value: Value) => WeakMap<object, Value>;", replacement = "const weakMapSet = (<Value>(map: WeakMap<object, Value>, key: object, value: Value) => WeakMap.prototype.set.call(map, key, value)) as <Value>(map: WeakMap<object, Value>, key: object, value: Value) => WeakMap<object, Value>;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_weakMapSet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_weakMapSetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_weakMapSetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_weakMapSetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_isFrozenContract = "finite batch plan captures Object.isFrozen in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_isFrozenMarker = "PACK_LOCAL_POOL_DRAW_STATE_ISFROZEN_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_isFrozenContract, async () => { console.log(pack_local_pool_draw_state_isFrozenMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Object, original = owner["isFrozen"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isFrozen"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isFrozen"] = original; } });
test("finite batch isFrozen capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isFrozen: typeof Object.isFrozen = Object.isFrozen;", replacement = "const isFrozen: typeof Object.isFrozen = (value) => Object.isFrozen(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_isFrozen-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_isFrozenContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_isFrozenMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_isFrozenContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_isSafeIntegerContract = "finite batch plan captures Number.isSafeInteger in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_isSafeIntegerMarker = "PACK_LOCAL_POOL_DRAW_STATE_ISSAFEINTEGER_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_isSafeIntegerContract, async () => { console.log(pack_local_pool_draw_state_isSafeIntegerMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Number, original = owner["isSafeInteger"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isSafeInteger"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["isSafeInteger"] = original; } });
test("finite batch isSafeInteger capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isSafeInteger: typeof Number.isSafeInteger = Number.isSafeInteger;", replacement = "const isSafeInteger: typeof Number.isSafeInteger = (value) => Number.isSafeInteger(value);"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_isSafeInteger-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_isSafeIntegerContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_isSafeIntegerMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_isSafeIntegerContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_ISSAFEINTEGER_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_arrayEveryContract = "finite batch plan captures Array.prototype.every in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_arrayEveryMarker = "PACK_LOCAL_POOL_DRAW_STATE_ARRAYEVERY_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_arrayEveryContract, async () => { console.log(pack_local_pool_draw_state_arrayEveryMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array.prototype, original = owner["every"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["every"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["every"] = original; } });
test("finite batch arrayEvery capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arrayEvery = Function.prototype.call.bind(Array.prototype.every) as <Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => boolean;", replacement = "const arrayEvery = (<Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => Array.prototype.every.call(array, predicate)) as <Value>(array: readonly Value[], predicate: (value: Value, index: number) => boolean) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_arrayEvery-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_arrayEveryContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_arrayEveryMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_arrayEveryContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_ARRAYEVERY_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_arrayMapContract = "finite batch plan captures Array.prototype.map in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_arrayMapMarker = "PACK_LOCAL_POOL_DRAW_STATE_ARRAYMAP_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_arrayMapContract, async () => { console.log(pack_local_pool_draw_state_arrayMapMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Array.prototype, original = owner["map"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["map"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_ARRAYMAP_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_ARRAYMAP_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_ARRAYMAP_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["map"] = original; } });
test("finite batch arrayMap capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const arrayMap = Function.prototype.call.bind(Array.prototype.map) as <Value, Result>(array: readonly Value[], callback: (value: Value, index: number) => Result) => Result[];", replacement = "const arrayMap = (<Value, Result>(array: readonly Value[], callback: (value: Value, index: number) => Result) => Array.prototype.map.call(array, callback)) as <Value, Result>(array: readonly Value[], callback: (value: Value, index: number) => Result) => Result[];"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_arrayMap-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_arrayMapContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_arrayMapMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_arrayMapContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_ARRAYMAP_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_weakSetAddContract = "finite batch plan captures WeakSet.prototype.add in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_weakSetAddMarker = "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETADD_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_weakSetAddContract, async () => { console.log(pack_local_pool_draw_state_weakSetAddMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakSet.prototype, original = owner["add"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["add"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETADD_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETADD_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETADD_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["add"] = original; } });
test("finite batch weakSetAdd capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakSetAdd = Function.prototype.call.bind(WeakSet.prototype.add) as (set: WeakSet<object>, value: object) => WeakSet<object>;", replacement = "const weakSetAdd = ((set: WeakSet<object>, value: object) => WeakSet.prototype.add.call(set, value)) as (set: WeakSet<object>, value: object) => WeakSet<object>;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_weakSetAdd-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_weakSetAddContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_weakSetAddMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_weakSetAddContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_WEAKSETADD_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_weakSetHasContract = "finite batch plan captures WeakSet.prototype.has in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_weakSetHasMarker = "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETHAS_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_weakSetHasContract, async () => { console.log(pack_local_pool_draw_state_weakSetHasMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakSet.prototype, original = owner["has"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["has"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["has"] = original; } });
test("finite batch weakSetHas capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakSetHas = Function.prototype.call.bind(WeakSet.prototype.has) as (set: WeakSet<object>, value: object) => boolean;", replacement = "const weakSetHas = ((set: WeakSet<object>, value: object) => WeakSet.prototype.has.call(set, value)) as (set: WeakSet<object>, value: object) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_weakSetHas-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_weakSetHasContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_weakSetHasMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_weakSetHasContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_WEAKSETHAS_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_weakMapSetContract = "finite batch plan captures WeakMap.prototype.set in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_weakMapSetMarker = "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPSET_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_weakMapSetContract, async () => { console.log(pack_local_pool_draw_state_weakMapSetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakMap.prototype, original = owner["set"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["set"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["set"] = original; } });
test("finite batch weakMapSet capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakMapSet = Function.prototype.call.bind(WeakMap.prototype.set) as (map: WeakMap<object, OmensCollationWeightTables>, key: object, value: OmensCollationWeightTables) => WeakMap<object, OmensCollationWeightTables>;", replacement = "const weakMapSet = ((map: WeakMap<object, OmensCollationWeightTables>, key: object, value: OmensCollationWeightTables) => WeakMap.prototype.set.call(map, key, value)) as (map: WeakMap<object, OmensCollationWeightTables>, key: object, value: OmensCollationWeightTables) => WeakMap<object, OmensCollationWeightTables>;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_weakMapSet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_weakMapSetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_weakMapSetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_weakMapSetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPSET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_weakMapGetContract = "finite batch plan captures WeakMap.prototype.get in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_weakMapGetMarker = "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPGET_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_weakMapGetContract, async () => { console.log(pack_local_pool_draw_state_weakMapGetMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = WeakMap.prototype, original = owner["get"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["get"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["get"] = original; } });
test("finite batch weakMapGet capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const weakMapGet = Function.prototype.call.bind(WeakMap.prototype.get) as (map: WeakMap<object, OmensCollationWeightTables>, key: object) => OmensCollationWeightTables | undefined;", replacement = "const weakMapGet = ((map: WeakMap<object, OmensCollationWeightTables>, key: object) => WeakMap.prototype.get.call(map, key)) as (map: WeakMap<object, OmensCollationWeightTables>, key: object) => OmensCollationWeightTables | undefined;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_weakMapGet-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_weakMapGetContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_weakMapGetMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_weakMapGetContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_WEAKMAPGET_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_setConstructorContract = "finite batch plan captures global Set constructor in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_setConstructorMarker = "PACK_LOCAL_POOL_DRAW_STATE_SETCONSTRUCTOR_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_setConstructorContract, async () => { console.log(pack_local_pool_draw_state_setConstructorMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = globalThis, original = owner["Set"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["Set"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_SETCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_SETCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_SETCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["Set"] = original; } });
test("finite batch setConstructor capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "new setConstructor<OfficialIdentityReference>()", replacement = "new globalThis.Set<OfficialIdentityReference>()"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_setConstructor-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_setConstructorContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_setConstructorMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_setConstructorContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_SETCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const pack_local_pool_draw_state_setHasContract = "finite batch plan captures Set.prototype.has in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_setHasMarker = "PACK_LOCAL_POOL_DRAW_STATE_SETHAS_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_setHasContract, async () => { console.log(pack_local_pool_draw_state_setHasMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Set.prototype, original = owner["has"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["has"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_SETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_SETHAS_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_SETHAS_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["has"] = original; } });
test("finite batch setHas capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const setHas = Function.prototype.call.bind(Set.prototype.has) as <Value>(set: Set<Value>, value: Value) => boolean;", replacement = "const setHas = (<Value>(set: Set<Value>, value: Value) => Set.prototype.has.call(set, value)) as <Value>(set: Set<Value>, value: Value) => boolean;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_setHas-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_setHasContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_setHasMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_setHasContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_SETHAS_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });

const collation_weight_tables_freezeContract = "finite batch plan captures Object.freeze in collation-weight-tables.ts", collation_weight_tables_freezeMarker = "COLLATION_WEIGHT_TABLES_FREEZE_CONTRACT_EXECUTED";
test(collation_weight_tables_freezeContract, async () => {
  console.log(collation_weight_tables_freezeMarker);
  const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl);
  const [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]);
  const capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }), owner = Object, original = owner["freeze"];
  let result;
  try {
    const samples = [0];
    Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["freeze"] = () => { throw new Error("poisoned"); }; return 0; } });
    assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "COLLATION_WEIGHT_TABLES_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.equal(result.state, "selected", "COLLATION_WEIGHT_TABLES_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "COLLATION_WEIGHT_TABLES_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
  } finally { owner["freeze"] = original; }
});
test("finite batch freeze capture mutation in collation-weight-tables.ts fails its exact named contract", () => {
  const sourcePath = new URL("../src/collation-weight-tables.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const freeze: typeof Object.freeze = Object.freeze;", replacement = "const freeze: typeof Object.freeze = ((value: object) => Object.freeze(value)) as typeof Object.freeze;";
  assert.equal(original.split(anchor).length - 1, 1);
  const mutated = original.replace(anchor, replacement);
  assert.notEqual(mutated, original);
  let snapshot;
  try {
    snapshot = mkdtempSync(join(tmpdir(), "draft-table-collation_weight_tables_freeze-"));
    const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url));
    for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile));
    symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir");
    writeFileSync(join(snapshot, "collation-weight-tables.ts"), mutated);
    writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}');
    const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" });
    assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr);
    const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href };
    delete environment.NODE_TEST_CONTEXT;
    const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(collation_weight_tables_freezeContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u);
    assert.equal(result.status, 1, result.stdout + "\n" + result.stderr);
    assert.equal(lines.filter((line) => line === "# " + collation_weight_tables_freezeMarker).length, 1);
    assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(collation_weight_tables_freezeContract)).length, 1);
    assert.equal(lines.filter((line) => line.includes("COLLATION_WEIGHT_TABLES_FREEZE_MUST_SURVIVE_HOSTILE_GETTER")).length, 1);
  } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); }
});

const pack_collation_plan_freezeContract = "finite batch plan captures Object.freeze in pack-collation-plan.ts", pack_collation_plan_freezeMarker = "PACK_COLLATION_PLAN_FREEZE_CONTRACT_EXECUTED";
test(pack_collation_plan_freezeContract, async () => {
  console.log(pack_collation_plan_freezeMarker);
  const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl);
  const [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]);
  const capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }), owner = Object, original = owner["freeze"];
  let result;
  try {
    const samples = [0];
    Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["freeze"] = () => { throw new Error("poisoned"); }; return 0; } });
    assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
  } finally { owner["freeze"] = original; }
});
test("finite batch freeze capture mutation in pack-collation-plan.ts fails its exact named contract", () => {
  const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const freeze: typeof Object.freeze = Object.freeze;", replacement = "const freeze: typeof Object.freeze = ((value: object) => Object.freeze(value)) as typeof Object.freeze;";
  assert.equal(original.split(anchor).length - 1, 1);
  const mutated = original.replace(anchor, replacement);
  assert.notEqual(mutated, original);
  let snapshot;
  try {
    snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_freeze-"));
    const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url));
    for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile));
    symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir");
    writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated);
    writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}');
    const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" });
    assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr);
    const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href };
    delete environment.NODE_TEST_CONTEXT;
    const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_freezeContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u);
    assert.equal(result.status, 1, result.stdout + "\n" + result.stderr);
    assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_freezeMarker).length, 1);
    assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_freezeContract)).length, 1);
    assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_FREEZE_MUST_SURVIVE_HOSTILE_GETTER")).length, 1);
  } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); }
});

const pack_collation_plan_isFrozenContract = "finite batch plan captures Object.isFrozen in pack-collation-plan.ts", pack_collation_plan_isFrozenMarker = "PACK_COLLATION_PLAN_ISFROZEN_CONTRACT_EXECUTED";
test(pack_collation_plan_isFrozenContract, async () => {
  console.log(pack_collation_plan_isFrozenMarker);
  const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl);
  const [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]);
  const capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }), owner = Object, original = owner["isFrozen"];
  let result;
  try {
    const samples = [0];
    Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["isFrozen"] = () => { throw new Error("poisoned"); }; return 0; } });
    assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER");
    assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER");
    assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER");
  } finally { owner["isFrozen"] = original; }
});
test("finite batch isFrozen capture mutation in pack-collation-plan.ts fails its exact named contract", () => {
  const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const isFrozen: typeof Object.isFrozen = Object.isFrozen;", replacement = "const isFrozen: typeof Object.isFrozen = (value) => Object.isFrozen(value);";
  assert.equal(original.split(anchor).length - 1, 1);
  const mutated = original.replace(anchor, replacement);
  assert.notEqual(mutated, original);
  let snapshot;
  try {
    snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_isFrozen-"));
    const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url));
    for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile));
    symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir");
    writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated);
    writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}');
    const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" });
    assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr);
    const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href };
    delete environment.NODE_TEST_CONTEXT;
    const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_isFrozenContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u);
    assert.equal(result.status, 1, result.stdout + "\n" + result.stderr);
    assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_isFrozenMarker).length, 1);
    assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_isFrozenContract)).length, 1);
    assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_ISFROZEN_MUST_SURVIVE_HOSTILE_GETTER")).length, 1);
  } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); }
});

const pack_collation_plan_mapConstructorContract = "finite batch plan captures global Map constructor in pack-collation-plan.ts", pack_collation_plan_mapConstructorMarker = "PACK_COLLATION_PLAN_MAPCONSTRUCTOR_CONTRACT_EXECUTED";
test(pack_collation_plan_mapConstructorContract, async () => {
  console.log(pack_collation_plan_mapConstructorMarker);
  const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl);
  const [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]);
  const capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }), owner = globalThis, original = owner["Map"];
  let result;
  try {
    const samples = [0];
    Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["Map"] = function () { throw new Error("poisoned"); }; return 0; } });
    assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_COLLATION_PLAN_MAPCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER");
    assert.equal(result.state, "selected", "PACK_COLLATION_PLAN_MAPCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER");
    assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_COLLATION_PLAN_MAPCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER");
  } finally { owner["Map"] = original; }
});
test("finite batch mapConstructor capture mutation in pack-collation-plan.ts fails its exact named contract", () => {
  const sourcePath = new URL("../src/pack-collation-plan.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const mapConstructor: typeof Map = Map;", replacement = "const mapConstructor: typeof Map = new Proxy(Map, { construct(_target, values) { return Reflect.construct(globalThis.Map, values); } });";
  assert.equal(original.split(anchor).length - 1, 1);
  const mutated = original.replace(anchor, replacement);
  assert.notEqual(mutated, original);
  let snapshot;
  try {
    snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_collation_plan_mapConstructor-"));
    const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url));
    for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile));
    symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir");
    writeFileSync(join(snapshot, "pack-collation-plan.ts"), mutated);
    writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}');
    const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" });
    assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr);
    const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href };
    delete environment.NODE_TEST_CONTEXT;
    const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_collation_plan_mapConstructorContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u);
    assert.equal(result.status, 1, result.stdout + "\n" + result.stderr);
    assert.equal(lines.filter((line) => line === "# " + pack_collation_plan_mapConstructorMarker).length, 1);
    assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_collation_plan_mapConstructorContract)).length, 1);
    assert.equal(lines.filter((line) => line.includes("PACK_COLLATION_PLAN_MAPCONSTRUCTOR_MUST_SURVIVE_HOSTILE_GETTER")).length, 1);
  } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); }
});

const pack_local_pool_draw_state_freezeContract = "finite batch plan captures Object.freeze in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_freezeMarker = "PACK_LOCAL_POOL_DRAW_STATE_FREEZE_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_freezeContract, async () => {
  console.log(pack_local_pool_draw_state_freezeMarker);
  const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl);
  const [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]);
  const capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }), owner = Object, original = owner["freeze"];
  let result;
  try {
    const samples = [0];
    Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["freeze"] = () => { throw new Error("poisoned"); }; return 0; } });
    assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
    assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_FREEZE_MUST_SURVIVE_HOSTILE_GETTER");
  } finally { owner["freeze"] = original; }
});
test("finite batch freeze capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => {
  const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const freeze: typeof Object.freeze = Object.freeze;", replacement = "const freeze: typeof Object.freeze = ((value: object) => Object.freeze(value)) as typeof Object.freeze;";
  assert.equal(original.split(anchor).length - 1, 1);
  const mutated = original.replace(anchor, replacement);
  assert.notEqual(mutated, original);
  let snapshot;
  try {
    snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_freeze-"));
    const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url));
    for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile));
    symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir");
    writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated);
    writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}');
    const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" });
    assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr);
    const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href };
    delete environment.NODE_TEST_CONTEXT;
    const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_freezeContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u);
    assert.equal(result.status, 1, result.stdout + "\n" + result.stderr);
    assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_freezeMarker).length, 1);
    assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_freezeContract)).length, 1);
    assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_FREEZE_MUST_SURVIVE_HOSTILE_GETTER")).length, 1);
  } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); }
});

const pack_local_pool_draw_state_setAddContract = "finite batch plan captures Set.prototype.add in pack-local-pool-draw-state.ts", pack_local_pool_draw_state_setAddMarker = "PACK_LOCAL_POOL_DRAW_STATE_SETADD_CONTRACT_EXECUTED";
test(pack_local_pool_draw_state_setAddContract, async () => { console.log(pack_local_pool_draw_state_setAddMarker); const moduleUrl = process.env[moduleKey] ?? finiteSourcePath.href, directory = new URL("./", moduleUrl), [plan, custom, eligibility, layouts, upstream, pools, identity, layoutResolution, poolResolution, compiler, packPlan] = await Promise.all([import(moduleUrl), import(new URL("custom-cards.ts", directory)), import(new URL("draft-eligibility-classification.ts", directory)), import(new URL("layouts.ts", directory)), import(new URL("official-upstream-id-reconciliation.ts", directory)), import(new URL("pools.ts", directory)), import(new URL("recipe-official-identity-reconciliation.ts", directory)), import(new URL("recipe-layout-pool-resolution.ts", directory)), import(new URL("recipe-pool-identity-resolution.ts", directory)), import(new URL("collation-weight-tables.ts", directory)), import(new URL("pack-collation-plan.ts", directory))]), capabilities = fictionalCollationCapabilities({ ...custom, ...eligibility, ...layouts, ...upstream, ...pools, ...identity, ...layoutResolution, ...poolResolution, ...compiler }); const owner = Set.prototype, original = owner["add"]; let result; try { const samples = [0]; Object.defineProperty(samples, 0, { configurable: true, enumerable: true, get() { owner["add"] = () => { throw new Error("poisoned"); }; return 0; } }); assert.doesNotThrow(() => { result = plan.initializeOmensPackCollationPlanFromUnsigned32SampleBatch(capabilities.tables, samples); }, "PACK_LOCAL_POOL_DRAW_STATE_SETADD_MUST_SURVIVE_HOSTILE_GETTER"); assert.equal(result.state, "selected", "PACK_LOCAL_POOL_DRAW_STATE_SETADD_MUST_SURVIVE_HOSTILE_GETTER"); assert.doesNotThrow(() => { assert.equal(packPlan.readOmensPackCollationPlanNextPositionForTransition(result.plan), 0); }, "PACK_LOCAL_POOL_DRAW_STATE_SETADD_MUST_SURVIVE_HOSTILE_GETTER"); } finally { owner["add"] = original; } });
test("finite batch setAdd capture mutation in pack-local-pool-draw-state.ts fails its exact named contract", () => { const sourcePath = new URL("../src/pack-local-pool-draw-state.ts", import.meta.url), original = readFileSync(sourcePath, "utf8"), anchor = "const setAdd = Function.prototype.call.bind(Set.prototype.add) as <Value>(set: Set<Value>, value: Value) => Set<Value>;", replacement = "const setAdd = (<Value>(set: Set<Value>, value: Value) => Set.prototype.add.call(set, value)) as <Value>(set: Set<Value>, value: Value) => Set<Value>;"; assert.equal(original.split(anchor).length - 1, 1); const mutated = original.replace(anchor, replacement); assert.notEqual(mutated, original); let snapshot; try { snapshot = mkdtempSync(join(tmpdir(), "draft-table-pack_local_pool_draw_state_setAdd-")); const sourceDirectory = fileURLToPath(new URL("../src/", import.meta.url)); for (const sourceFile of readdirSync(sourceDirectory).filter((candidate) => candidate.endsWith(".ts"))) copyFileSync(join(sourceDirectory, sourceFile), join(snapshot, sourceFile)); symlinkSync(join(sourceDirectory, "../../../node_modules"), join(snapshot, "node_modules"), "dir"); writeFileSync(join(snapshot, "pack-local-pool-draw-state.ts"), mutated); writeFileSync(join(snapshot, "tsconfig.json"), '{"compilerOptions":{"target":"ES2022","module":"ES2022","moduleResolution":"bundler","strict":true,"noEmit":true,"allowImportingTsExtensions":true},"include":["*.ts"]}'); const typecheck = spawnSync(join(snapshot, "node_modules", ".bin", "tsc"), ["-p", join(snapshot, "tsconfig.json")], { encoding: "utf8" }); assert.equal(typecheck.status, 0, typecheck.stdout + "\n" + typecheck.stderr); const environment = { ...process.env, [moduleKey]: pathToFileURL(join(snapshot, "finite-batch-collation-plan.ts")).href }; delete environment.NODE_TEST_CONTEXT; const result = spawnSync(process.execPath, ["--experimental-strip-types", "--test", "--test-name-pattern", exactPattern(pack_local_pool_draw_state_setAddContract), fileURLToPath(import.meta.url)], { encoding: "utf8", env: environment }), lines = result.stdout.split(/\r?\n/u); assert.equal(result.status, 1, result.stdout + "\n" + result.stderr); assert.equal(lines.filter((line) => line === "# " + pack_local_pool_draw_state_setAddMarker).length, 1); assert.equal(lines.filter((line) => line.startsWith("not ok ") && line.includes(pack_local_pool_draw_state_setAddContract)).length, 1); assert.equal(lines.filter((line) => line.includes("PACK_LOCAL_POOL_DRAW_STATE_SETADD_MUST_SURVIVE_HOSTILE_GETTER")).length, 1); } finally { if (snapshot !== undefined) rmSync(snapshot, { recursive: true, force: true }); } });
